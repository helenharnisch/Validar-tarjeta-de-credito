
//PRUEBA DE TRABAJO
/*function isValidCard(validar){
  var pregunta = parseInt(prompt("Ingrese numero"));  //preguntar al usuario por numeros de tarjeta a verificar
  var nuevoarray = [];
  var arrayinvertido = nuevoarray.reverse();
  var suma = 0
  var digitos = validar.length
  For (var i = 0; i < digitos; i++){

  }
}
*/
function  isValidCard(){
  do{
    var digitos = parseInt(prompt("Ingresa tu numero de tarjeta")); //preguntar al usuario por numeros de tarjeta a verificar
 } while (!digitos);

 for (i = 0; i < validar.length; i++);{
  var digitos = validar.length;
  var nuevoarray = [];
  var arrayinvertido = nuevoarray.reverse();
  var digitos = validar.length;
  var result = (digitos[i] + nuevoarray);

 }

 	return alert( "tu arrayinvertido es " + " " + arrayinvertido);
}

 isValidCard();
